{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply gymfolio-d1-dev --local",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "migrate:staging": "wrangler d1 migrations apply gymfolio-d1-staging --remote --env staging"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "aws4fetch": "^1.0.20",
    "db": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.4",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "@types/node": "^26.3.0",
    "typescript": "~6.0.2",
    "wrangler": "^4.125.0"
  }
}
