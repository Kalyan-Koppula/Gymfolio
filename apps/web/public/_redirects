# SPA fallback for Cloudflare Pages client routes.
# /api/* is handled by Pages Functions (functions/api/[[path]].ts), which take
# precedence over this rewrite when the Functions bundle is on the deployment.
/*    /index.html   200
